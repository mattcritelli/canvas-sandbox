var canvas = document.querySelector('canvas');
canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

// c stands for context
var c = canvas.getContext('2d');

//Rectangles
// c.fillStyle = ('rgba(0, 200, 200, 0.9)');
// c.fillRect(200, 100, 100, 200);

// c.fillStyle = ('rgba(200, 0, 200, 0.9)');
// c.fillRect(500, 250, 150, 300);

// Line
// c.beginPath();
// c.moveTo(700, 300);
// c.lineTo(50, 600);
// c.lineTo(150, 600);
// c.strokeStyle = "#7a76a6";
// c.stroke();

// Arc/Circle
// ctx.arc(x, y, radius, startAngle(radians), endAngle(radians) [, anticlockwise]);
// c.beginPath();
// c.arc(400, 400, 300, 0, Math.PI * 2, false);

// c.strokeStyle = "blue";
// c.stroke();

// for loops

// for (var i = 0; i < 10000; i++){
    // var x = Math.random() * window.innerWidth;
    // var y = Math.random() * window.innerHeight;
    // var rad = Math.random() * 10;
//   c.beginPath();
//   c.arc(x, y, rad, 0, Math.PI * 2, false);
//   c.strokeStyle = "blue";
//   c.fillStyle = "green";
//   c.fill();
//   c.stroke();
// }
